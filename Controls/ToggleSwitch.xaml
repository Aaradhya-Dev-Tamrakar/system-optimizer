<UserControl x:Class="NovaOptimizer.Controls.ToggleSwitch"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Cursor="Hand" MouseLeftButtonDown="Toggle_Click">
    <Grid VerticalAlignment="Center">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- The Sliding Track and Thumb -->
        <Border x:Name="Track" Grid.Column="0" Width="44" Height="24" CornerRadius="12"
                Background="{StaticResource BgCardSubtle}" BorderBrush="{StaticResource BorderCard}" BorderThickness="1.5">
            <Canvas Width="44" Height="24">
                <Ellipse x:Name="Thumb" Width="16" Height="16" Fill="{StaticResource TextSecondary}" Canvas.Top="2.5" Canvas.Left="3">
                    <Ellipse.Effect>
                        <DropShadowEffect BlurRadius="4" ShadowDepth="1" Direction="270" Opacity="0.3"/>
                    </Ellipse.Effect>
                </Ellipse>
            </Canvas>
        </Border>

        <!-- Optional Header/Label -->
        <TextBlock x:Name="TxtHeader" Grid.Column="1" Margin="8,0,0,0" VerticalAlignment="Center"
                   FontSize="13" Foreground="{StaticResource TextPrimary}" Visibility="Collapsed"/>
    </Grid>
</UserControl>
