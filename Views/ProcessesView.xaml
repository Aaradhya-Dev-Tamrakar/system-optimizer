<UserControl x:Class="NovaOptimizer.Views.ProcessesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="850">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Text="TASK MANAGER" FontSize="11" FontWeight="Bold" Foreground="{StaticResource AccentCyan}" Margin="0,0,0,4"/>
        <TextBlock Grid.Row="1" Text="Running Processes" FontSize="24" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" Margin="0,0,0,16"/>

        <!-- Controls Toolbar -->
        <Grid Grid.Row="1" VerticalAlignment="Bottom" HorizontalAlignment="Right" Margin="0,0,0,16">
            <StackPanel Orientation="Horizontal">
                <!-- Search Box -->
                <TextBox x:Name="TxtSearch" Width="240" Style="{StaticResource DarkTextBox}" Margin="0,0,12,0" 
                         TextChanged="TxtSearch_TextChanged" Tag="Search process name or PID..."/>
                <!-- Quick Kill -->
                <Button x:Name="BtnEndTask" Content="End Task" Style="{StaticResource DangerButton}" Click="BtnEndTask_Click" Margin="0,0,8,0"/>
                <!-- Trim Working Set -->
                <Button x:Name="BtnTrimProcess" Content="Trim RAM" Style="{StaticResource SecondaryButton}" Click="BtnTrimProcess_Click"/>
            </StackPanel>
        </Grid>

        <!-- Processes DataGrid -->
        <Border Grid.Row="2" Style="{StaticResource CardPanel}" Padding="0">
            <DataGrid x:Name="DgProcesses" AutoGenerateColumns="False" IsReadOnly="True"
                      Background="Transparent" BorderThickness="0" RowBackground="#1A1D27" AlternatingRowBackground="#161822"
                      GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="{StaticResource BorderCard}"
                      Foreground="{StaticResource TextPrimary}" HeadersVisibility="Column" SelectionMode="Single"
                      CanUserSortColumns="True" SelectionChanged="DgProcesses_SelectionChanged">
                
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#14161F"/>
                        <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="FontSize" Value="12"/>
                        <Setter Property="Padding" Value="12,10"/>
                        <Setter Property="BorderBrush" Value="{StaticResource BorderCard}"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>

                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Height" Value="34"/>
                        <Setter Property="FontSize" Value="12.5"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource BgCardHover}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#252F48"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>

                <DataGrid.ContextMenu>
                    <ContextMenu Background="#1A1D27" Foreground="{StaticResource TextPrimary}" BorderBrush="{StaticResource BorderCard}">
                        <MenuItem Header="❌ End Task" Click="MenuEndTask_Click"/>
                        <MenuItem Header="⚡ Kill Process Tree" Click="MenuKillTree_Click"/>
                        <Separator Background="{StaticResource BorderCard}"/>
                        <MenuItem Header="🧹 Trim Working Set (Clean RAM)" Click="MenuTrimRam_Click"/>
                        <MenuItem Header="📈 Set Priority">
                            <MenuItem Header="Realtime" Click="MenuPriority_Click" Tag="Realtime"/>
                            <MenuItem Header="High" Click="MenuPriority_Click" Tag="High"/>
                            <MenuItem Header="Above Normal" Click="MenuPriority_Click" Tag="AboveNormal"/>
                            <MenuItem Header="Normal" Click="MenuPriority_Click" Tag="Normal"/>
                            <MenuItem Header="Below Normal" Click="MenuPriority_Click" Tag="BelowNormal"/>
                            <MenuItem Header="Idle" Click="MenuPriority_Click" Tag="Idle"/>
                        </MenuItem>
                        <Separator Background="{StaticResource BorderCard}"/>
                        <MenuItem Header="📂 Open File Location" Click="MenuOpenLocation_Click"/>
                    </ContextMenu>
                </DataGrid.ContextMenu>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Process Name" Binding="{Binding Name}" Width="200"/>
                    <DataGridTextColumn Header="PID" Binding="{Binding Id}" Width="80"/>
                    <DataGridTextColumn Header="RAM (MB)" Binding="{Binding DisplayRam}" Width="110"/>
                    <DataGridTextColumn Header="CPU %" Binding="{Binding DisplayCpu}" Width="90"/>
                    <DataGridTextColumn Header="Priority" Binding="{Binding Priority}" Width="100"/>
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="90"/>
                    <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- Footer status -->
        <Grid Grid.Row="3" Margin="0,12,0,0">
            <TextBlock x:Name="TxtStatus" Text="Processes: 0" FontSize="12" Foreground="{StaticResource TextMuted}"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <TextBlock Text="Right-click any process for advanced Task Manager controls" FontSize="11.5" Foreground="{StaticResource TextMuted}"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
