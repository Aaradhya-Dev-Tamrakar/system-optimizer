<UserControl x:Class="NovaOptimizer.Views.PerformanceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="850">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="24" MaxWidth="900">
            <!-- Header -->
            <TextBlock Text="PERFORMANCE MONITOR" FontSize="11" FontWeight="Bold" Foreground="{StaticResource AccentCyan}" Margin="0,0,0,4"/>
            <TextBlock Text="System Resources &amp; Hardware" FontSize="24" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" Margin="0,0,0,24"/>

            <!-- Main Resource Cards -->
            <Grid Margin="0,0,0,24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- CPU Card -->
                <Border Grid.Column="0" Style="{StaticResource CardPanel}">
                    <StackPanel>
                        <Grid Margin="0,0,0,12">
                            <TextBlock Text="CPU Utilization" FontSize="16" FontWeight="Bold" Foreground="{StaticResource TextPrimary}"/>
                            <TextBlock x:Name="TxtCpuOverall" Text="12%" FontSize="24" FontWeight="Bold" Foreground="{StaticResource AccentCyan}" HorizontalAlignment="Right"/>
                        </Grid>
                        <ProgressBar x:Name="PbCpu" Height="8" Background="#14161F" Foreground="{StaticResource AccentCyan}" BorderThickness="0" Value="12" Margin="0,0,0,16"/>

                        <StackPanel Margin="0,0,0,8">
                            <TextBlock x:Name="TxtProcessorName" Text="Intel / AMD Processor" FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource TextPrimary}"/>
                            <TextBlock x:Name="TxtCores" Text="Logical Cores: 8" FontSize="12" Foreground="{StaticResource TextSecondary}" Margin="0,2,0,0"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- RAM Card -->
                <Border Grid.Column="2" Style="{StaticResource CardPanel}">
                    <StackPanel>
                        <Grid Margin="0,0,0,12">
                            <TextBlock Text="Memory Usage" FontSize="16" FontWeight="Bold" Foreground="{StaticResource TextPrimary}"/>
                            <TextBlock x:Name="TxtRamOverall" Text="45%" FontSize="24" FontWeight="Bold" Foreground="{StaticResource AccentPurple}" HorizontalAlignment="Right"/>
                        </Grid>
                        <ProgressBar x:Name="PbRam" Height="8" Background="#14161F" Foreground="{StaticResource AccentPurple}" BorderThickness="0" Value="45" Margin="0,0,0,16"/>

                        <StackPanel Margin="0,0,0,8">
                            <TextBlock x:Name="TxtRamTotals" Text="7.2 / 16.0 GB Installed" FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource TextPrimary}"/>
                            <TextBlock x:Name="TxtRamAvailable" Text="Available: 8.8 GB" FontSize="12" Foreground="{StaticResource TextSecondary}" Margin="0,2,0,0"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Detailed Specifications Card -->
            <Border Style="{StaticResource CardPanel}">
                <StackPanel>
                    <TextBlock Text="KERNEL &amp; MEMORY ARCHITECTURE" FontSize="11" FontWeight="Bold" Foreground="{StaticResource AccentGreen}" Margin="0,0,0,8"/>
                    <TextBlock Text="Windows Performance Metrics" FontSize="16" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" Margin="0,0,0,16"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Left Column -->
                        <StackPanel Grid.Column="0" Margin="0,0,16,0">
                            <Border BorderBrush="{StaticResource BorderCard}" BorderThickness="0,0,0,1" Padding="0,6">
                                <Grid>
                                    <TextBlock Text="Committed Memory" Foreground="{StaticResource TextSecondary}"/>
                                    <TextBlock x:Name="TxtCommitted" Text="8.5 / 19.2 GB" Foreground="{StaticResource TextPrimary}" HorizontalAlignment="Right"/>
                                </Grid>
                            </Border>
                            <Border BorderBrush="{StaticResource BorderCard}" BorderThickness="0,0,0,1" Padding="0,6">
                                <Grid>
                                    <TextBlock Text="Cached Standby Memory" Foreground="{StaticResource TextSecondary}"/>
                                    <TextBlock x:Name="TxtCached" Text="4.1 GB" Foreground="{StaticResource AccentPurple}" HorizontalAlignment="Right"/>
                                </Grid>
                            </Border>
                            <Border BorderBrush="{StaticResource BorderCard}" BorderThickness="0,0,0,1" Padding="0,6">
                                <Grid>
                                    <TextBlock Text="Paged Pool" Foreground="{StaticResource TextSecondary}"/>
                                    <TextBlock x:Name="TxtPagedPool" Text="512 MB" Foreground="{StaticResource TextPrimary}" HorizontalAlignment="Right"/>
                                </Grid>
                            </Border>
                        </StackPanel>

                        <!-- Right Column -->
                        <StackPanel Grid.Column="1">
                            <Border BorderBrush="{StaticResource BorderCard}" BorderThickness="0,0,0,1" Padding="0,6">
                                <Grid>
                                    <TextBlock Text="Non-Paged Pool (Hardware)" Foreground="{StaticResource TextSecondary}"/>
                                    <TextBlock x:Name="TxtNonPagedPool" Text="420 MB" Foreground="{StaticResource TextPrimary}" HorizontalAlignment="Right"/>
                                </Grid>
                            </Border>
                            <Border BorderBrush="{StaticResource BorderCard}" BorderThickness="0,0,0,1" Padding="0,6">
                                <Grid>
                                    <TextBlock Text="Active Processes" Foreground="{StaticResource TextSecondary}"/>
                                    <TextBlock x:Name="TxtProcessCount" Text="185" Foreground="{StaticResource TextPrimary}" HorizontalAlignment="Right"/>
                                </Grid>
                            </Border>
                            <Border BorderBrush="{StaticResource BorderCard}" BorderThickness="0,0,0,1" Padding="0,6">
                                <Grid>
                                    <TextBlock Text="System Uptime" Foreground="{StaticResource TextSecondary}"/>
                                    <TextBlock x:Name="TxtUptime" Text="0d 05h 22m" Foreground="{StaticResource TextPrimary}" HorizontalAlignment="Right"/>
                                </Grid>
                            </Border>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
